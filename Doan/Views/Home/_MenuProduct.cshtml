@model IEnumerable<Doan.Models.categoryDetail>

    @if (Model != null && Model.Any())
    {
    <ul class="sidebar_categories">
        <li><a href="#" id="allCategories">Tất cả</a></li>
        @foreach (var item in Model)
        {
        <li><a href="#" class="categoryLink" data-categoryid="@item.catetory_id">@item.catetory_name</a></li>
        }
    </ul>

    }
